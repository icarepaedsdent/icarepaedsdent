# i-Care Paediatric Dentistry - Domain Configuration
# List domains in order of preference (one per line)
# The script will try each domain until it finds a working one

# Production domain (primary)
https://icarepaedsdent.com.au

# Current Vercel deployment (fallback)
https://i-care-one.vercel.app

# Local development (fallback)
http://localhost:3000 